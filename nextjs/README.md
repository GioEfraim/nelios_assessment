# Next.js frontend (Nelios assessment)

Docker setup, URLs, start/stop/reset, and WordPress activation are documented in the **[repository root README](../README.md)**.

## Run without Docker (optional)

WordPress must be reachable at `http://localhost:8080` (REST + plugin). From this folder:

```bash
npm ci
npm run dev
```

Then open http://localhost:3000.
